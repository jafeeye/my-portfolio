---
title: 企業共用設定
toc: true
date: 2026-04-06
---
### mac共用桌面
1.桌面資料夾設定共用
2.設定開啟smb共用並啟用登入帳號(下方可修改主機名稱)
![gh|330](https://raw.githubusercontent.com/jafeeye/imglib/main/obsidian/17591386650005k7c6m.png)
3.Win端登入
帳號：mac主機名\帳號 `mini.local\PIN`
密碼：mac主機密碼
關閉連線 `net use \\192.168.8.120 /delete` IPC$

## Win10 掛載NFS
1. `appwiz.cpl`啟用`Service for NFS/Client for NFS`
2. Synology 調整相對應權限，在Windows使用掛載命令
```
showmount -e \\192.168.8.x
mount \\192.168.8.x\目錄 z:
unmount -f z:
```
![](Pasted%20image%2020260406170629.png)

## WebDAV
Win 預設WebClient 服務僅支援 https 協議，而本地搭建的 webdav 服務和連結都是基於 http 協議的，因此造成了掛載失敗的情況，若要支援http需修改註冊表
`reg add "HKLM\SYSTEM\CurrentControlSet\Services\WebClient\Parameters" /v BasicAuthLevel /t REG_DWORD /d 2 /f`

> 以webdav協議不用webdav:// 只要打上https://即可

## AirPrint 印表機共享

安裝檔路徑： \\\192.168.8.106\20-系統設定\普通印表機啟用 AirPrint\
1. 安裝印表機驅動程式，開啟印表機共享
2. 安裝Bonjour
3. 安裝AirPrint installer
	- 點擊 Install AirPrint Service
	- 設置 Sercvie Startup 為Auto，點Start
	- 選擇Windows User Account，勾選允許空白及啟用來賓帳戶才能無輸入帳號密碼列印
	![gh|300](https://raw.githubusercontent.com/jafeeye/imglib/main/obsidian/1739971146000cs88gj.png)

4. 導入AirPrint IOS 5 Fix.reg
>實測一定要打上AirPrint IOS 5 Fix．否則將無法設定成功

成功畫面：
![gh|350](https://raw.githubusercontent.com/jafeeye/imglib/main/obsidian/1739627745000dm9z0d.png)


Windows 共享
如果無法共享在認證管理員儲存
![gh](https://raw.githubusercontent.com/jafeeye/imglib/main/obsidian/1742819159000r3q28b.png)

>[!Note] 實驗結果
>這個程式會導致macOS莫名無法儲存密碼列印，必須要每次輸入密碼才能原因不明，有可能是密碼多次儲存的關係 

### Windows
掛載指令
net share：顯示目前共用資料夾
`net use Z:\\192.168.8.106\Software /user:PIN Yster86a /persistent:no`
persistent: 不會自動連線
中斷網路磁碟機
`net use /delete *`
>在Windows中，Windows 檔案總管無法阻止它「自動嘗試連線」已掛載但尚未連線的 SMB 網路磁碟機(GPT)，


出現共享文件：目前的帳戶已被鎖定無法登陸
`cmdkey /add:ip位置 /user:用戶名 /pass:密碼`

Win11 無法共享原因 (提高安全性，預設不能用伺服端的權限帳戶強制要設密碼)
HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation\AllowInsecureGuestAuth =1 或 - 編輯「啟用不安全的來賓登入」原則，將其設定為「已啟用」。做gpudate
發生延伸錯誤 `Set-Smb Client Configuration -RequireSecuritySignature $false`

### SMB
我不知道大家有沒有用過window的net use這指令，他是一個可以自動連你的smb網路磁碟。最近我一直在玩他，但怕會忘記就來寫一下。
這是如果你電腦沒有設定密碼(空密碼)情況可以下以下
net use z: \\IP-ADDRESS\share-folder /user:username \login
有密碼:
net use X: \\server\share /user:user abc
你們也可以這樣下，沒有login，他會跳出來要你打帳號密碼。沒密碼就按enter鍵如下
net use z: \\IP-ADDRESS\share-folder
如果要清掉或取消連線可以用 \delete 如下:
net use z: /delete
列出所有網路磁碟: net use
可以不用指定drive letter，用* 當做temp如下:
net use * "\\server\my media" /persistent:no

